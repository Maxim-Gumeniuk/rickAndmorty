import React from 'react';

const initialValue = (param: number) => {
  () => {}; // eslint-disable-line @typescript-eslint/no-empty-function
}; // eslint-disable-line @typescript-eslint/no-empty-function
export const Context = React.createContext(initialValue); // eslint-disable-line @typescript-eslint/no-empty-function